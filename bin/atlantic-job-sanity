#!/usr/bin/env python

import sys
import json
import os.path

if len(sys.argv) < 2:
    print "Usage error: need job"
    sys.exit(1)

try:
    bld = json.load(open(sys.argv[1], 'r'))
except ValueError:
    sys.exit(3)

if bld == None:
    sys.exit(2)
